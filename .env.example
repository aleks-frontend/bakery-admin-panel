# The repo tracks .env.development (dev server) and .env.production (build).
# Create .env or .env.local to override (see https://vitejs.dev/guide/env-and-mode.html).
VITE_API_BASE_URL=https://n8n.aleksthecoder.com/
